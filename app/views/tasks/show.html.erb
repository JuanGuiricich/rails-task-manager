<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<a href="#"><%= @task.completed %></a>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
